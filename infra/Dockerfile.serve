FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn scikit-learn joblib numpy

COPY serve.py .

ENV PORT=8080
ENV MODEL_PATH=/models/current/model.pkl

EXPOSE ${PORT}

CMD ["python", "serve.py"]
